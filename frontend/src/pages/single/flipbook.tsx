import { ContentContainer } from "components/common"

const Flipbook = () => {
    return (
        <ContentContainer>
            <p>Flipbook</p>
        </ContentContainer>
    )
}

export default Flipbook;