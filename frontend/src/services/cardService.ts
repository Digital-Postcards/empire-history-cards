// If there are any functions or constants already defined, add export to them
// For example:
// export const getCard = (id: string) => { ... }

// If there are no functions or constants yet, add an empty export statement to make it a module
export {};
